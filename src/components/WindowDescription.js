import React, { useState } from "react";
import { connect } from "react-redux";

const WindowDescription = (props) => {
  return <div className="task-window-desc">Hi </div>;
};
export default connect()(WindowDescription);
