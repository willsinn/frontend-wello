import React from "react";

const BoardShareLink = props => {
  return (
    <ul className="board-menu-nav-list">
      <li className="board-menu-nav-item">
        <button className="board-menu-nav-btn">
          <span className="">Link to Share Board</span>
        </button>
      </li>
    </ul>
  );
};

export default BoardShareLink;
